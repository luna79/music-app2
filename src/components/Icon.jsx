import { useEffect, useState } from 'react';

const Icon = ({ name, size = 24, className = '', fill = 'none', strokeWidth = 2, ...props }) => {
  // ... tu código del componente Icon ...
};

export default Icon;